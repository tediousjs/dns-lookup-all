{
  "name": "dns-lookup-all",
  "version": "1.0.0",
  "description": "Like `dns.lookup` with the `all` option, but compatible with older Node.js verions.",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "author": "Arthur Schreiber",
  "license": "MIT",
  "dependencies": {
    "semver": "^5.3.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2"
  }
}
